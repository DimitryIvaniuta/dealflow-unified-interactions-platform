# dealflow-unified-interactions-platform
Spring Boot (Java 21) Dealflow platform with workspace RBAC, GraphQL SPQR (code-first schema), Flyway Postgres migrations, JPA Specifications filtering, and a unified Customer Timeline events feed (JSONB) with RBAC + integration-friendly mutations
